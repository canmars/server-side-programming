<%# 
    Hata Sayfası
    Genel hata mesajları için kullanılır
%>

<div class="error-page">
    <h1><%= title || 'Hata' %></h1>
    <p class="error-message"><%= message || 'Bir hata oluştu.' %></p>
    <div class="actions">
        <a href="/" class="btn btn-primary">Ana Sayfaya Dön</a>
        <a href="/users" class="btn btn-secondary">Kullanıcı Listesi</a>
    </div>
</div>

